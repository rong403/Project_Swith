<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="board">

	<!-- values에 정확히 어떤 것이 들어가야할지 잘 모름 -->
	<insert id="insertBoard" parameterType="BoardWrite" statementType="PREPARED">
		INSERT INTO BOARD (board_title, board_contents, member_id) VALUES
			(board_no.nextval, #{board_title}, #{board_contents}, #{member_id})
	</insert>
	
</mapper>
